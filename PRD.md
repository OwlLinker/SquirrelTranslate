# 产品需求

## 鼠须管候选翻译

- 在翻译开启时，在候选 comment 区显示译文；候选词与译文使用 TAB 分隔。
- 中文候选翻译为目标语言，英文候选翻译为中文；默认只显示第一候选，切换候选后动态显示当前选中项。
- 英文译文有音标时，在译文后显示音标；音标未返回时不显示占位内容。
- 已有缓存时直接显示，不访问网络；无缓存时异步查询，结果到达后刷新当前候选面板。
- Emoji 使用本机名称数据，不请求翻译接口。
- 候选面板激活时支持快捷键朗读当前候选词，以及输入当前候选词的翻译结果。
- 朗读快捷键朗读当前候选词的译文；译文尚未缓存时不朗读源词。
- 快捷键说明附加在当前候选页最后一个候选词后，不增加候选项、不影响翻页。
- 支持独立切换音标查询和显示，默认开启，并在提示中显示当前状态。
- 支持关闭鼠须管菜单栏输入法指示器，并稳定使用右 Shift 切换中英模式。
- 翻译服务支持独立配置多个提供者（Google、Bing、Sogou、Youdao 网页版、Youdao 开放平台、DeepL 网页版、DeepL API、彩云网页版、彩云 API）；提供者在启用且参数完整时按配置顺序参与查询，并支持失败回退。彩云 API 启用时不使用彩云网页版。
- 默认仅查询第一候选；当前运行配置查询前两个候选，因此两个候选均可显示各自音标。候选高亮切换时，音标跟随当前选中候选，已经取得的候选译文继续保留显示，新选中候选的译文异步补充。
- 箭头切换候选时，翻译和音标必须跟随鼠须管实际高亮项更新，不得固定显示第一候选。
- 支持配置自动翻译候选数量，默认 1，允许值限制为 1–9；当前选中的候选仍可动态触发翻译。
- 支持配置是否在候选词面板显示使用说明，默认显示。
- `translation.providers.yaml` 支持 `mac_dictionary.enabled` 本地词典开关；macOS 且启用时，本地词典优先于所有在线提供者。
- 本地词典启用时，所有候选词只查询本地词典并显示已有结果；当前选中的候选若本地无结果，才允许按在线提供者顺序补充，其他候选不触发网络补充。
- 本地词典未启用时，保持单候选规则：仅第一个候选自动查询和显示翻译。
- 本地词典查询成功后立即显示译文；选中候选的音标由原生扩展异步补查，优先读取本地英文词条，本地无音标时再在线补充。非选中候选不发起音标补查。
- 候选输入状态下按 Escape 只取消当前 Rime 组合，不将 Escape 传递给前台应用。
